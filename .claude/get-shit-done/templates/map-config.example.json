{
  "$schema": "https://json-schema.org/draft-07/schema",
  "description": "Custom exclusion patterns for codebase mapping",
  
  "_comment": "Copy this file to .planning/map-config.json to use custom exclusions",
  "_note": "Patterns are added to defaults: .claude, .github, .codex, node_modules, .git, dist, build, out, target, coverage",
  "_supports": "Glob patterns (* for wildcards, ** for recursive matching)",
  
  "exclude": [
    "vendor/",
    "third_party/",
    "*.generated.*",
    "**/__generated__/**"
  ]
}
